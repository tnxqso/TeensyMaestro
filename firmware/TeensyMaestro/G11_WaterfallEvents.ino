#include "tm_sketch_api.h"
void configureWaterfallEvents()
{
  for (int i = 0; i < fRig.nMaxWaterfall; i++)
  {
    // fRig.waterfall[i].attach_waterfall_event(onWaterfall_waterfall);
    // fRig.waterfall[i].attach_x_pixels_event(onWaterfall_x_pixels);
    // fRig.waterfall[i].attach_center_event(onWaterfall_center);
    // fRig.waterfall[i].attach_bandwidth_event(onWaterfall_bandwidth);
    // fRig.waterfall[i].attach_line_duration_event(onWaterfall_line_duration);
    // fRig.waterfall[i].attach_rfgain_event(onWaterfall_rfgain);
    // fRig.waterfall[i].attach_rxant_event(onWaterfall_rxant);
    // fRig.waterfall[i].attach_wide_event(onWaterfall_wide);
    // fRig.waterfall[i].attach_loopa_event(onWaterfall_loopa);
    // fRig.waterfall[i].attach_loopb_event(onWaterfall_loopb);
    // fRig.waterfall[i].attach_band_event(onWaterfall_band);
    // fRig.waterfall[i].attach_daxiq_event(onWaterfall_daxiq);
    // fRig.waterfall[i].attach_daxiq_rate_event(onWaterfall_daxiq_rate);
    // fRig.waterfall[i].attach_capacity_event(onWaterfall_capacity);
    // fRig.waterfall[i].attach_available_event(onWaterfall_available);
    // fRig.waterfall[i].attach_panadapter_event(onWaterfall_panadapter);
    // fRig.waterfall[i].attach_color_gain_event(onWaterfall_color_gain);
    // fRig.waterfall[i].attach_auto_black_event(onWaterfall_auto_black);
    // fRig.waterfall[i].attach_black_level_event(onWaterfall_black_level);
    // fRig.waterfall[i].attach_gradient_index_event(onWaterfall_gradient_index);
    // fRig.waterfall[i].attach_xvtr_event(onWaterfall_xvtr);
  }
}

// void onWaterfall_waterfall(const int senderId) {
// debug("onWaterfall_waterfall(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_x_pixels(const int senderId) {
// debug("onWaterfall_x_pixels(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_center(const int senderId) {
// debug("onWaterfall_center(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_bandwidth(const int senderId) {
// debug("onWaterfall_bandwidth(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_line_duration(const int senderId) {
//debug("onWaterfall_line_duration(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_rfgain(const int senderId) {
// debug("onWaterfall_rfgain(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_rxant(const int senderId) {
// debug("onWaterfall_rxant(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_wide(const int senderId) {
// debug("onWaterfall_wide(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_loopa(const int senderId) {
// debug("onWaterfall_loopa(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_loopb(const int senderId) {
// debug("onWaterfall_loopb(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_band(const int senderId) {
// debug("onWaterfall_band(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_daxiq(const int senderId) {
// debug("onWaterfall_daxiq(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_daxiq_rate(const int senderId) {
// debug("onWaterfall_daxiq_rate(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_capacity(const int senderId) {
// debug("onWaterfall_capacity(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_available(const int senderId) {
// debug("onWaterfall_available(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_panadapter(const int senderId) {
// debug("onWaterfall_panadapter(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_color_gain(const int senderId) {
//debug("onWaterfall_color_gain(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_auto_black(const int senderId) {
// debug("onWaterfall_auto_black(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_black_level(const int senderId) {
// debug("onWaterfall_black_level(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_gradient_index(const int senderId) {
// debug("onWaterfall_gradient_index(");   debug(senderId);  debugln(") event!");
// }

// void onWaterfall_xvtr(const int senderId) {
// debug("onWaterfall_xvtr(");   debug(senderId);  debugln(") event!");
// }
